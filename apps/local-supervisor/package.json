{
  "name": "model-worklog-supervisor",
  "version": "0.1.14",
  "description": "Local runtime for durable Model Logger sessions",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ManoharEldhandi/model-worklog.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "model-worklog-supervisor": "dist/main.js"
  },
  "scripts": {
    "clean": "tsc -b --clean && node -e \"for (const path of ['dist', 'test-dist', 'tsconfig.tsbuildinfo', 'tsconfig.test.tsbuildinfo']) require('node:fs').rmSync(path, { recursive: true, force: true })\"",
    "build": "npm run clean && tsc -b",
    "start": "node dist/main.js",
    "prepack": "npm run build",
    "test": "npm run build && tsc -p tsconfig.test.json && node --enable-source-maps --test \"test-dist/**/*.test.js\""
  },
  "dependencies": {
    "model-worklog-sdk": "^0.1.14",
    "model-worklog-schema": "^0.1.14"
  },
  "engines": {
    "node": ">=22"
  }
}
