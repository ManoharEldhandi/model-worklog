{
  "name": "model-worklog-sdk",
  "version": "0.1.0",
  "description": "Local integration client for Model Logger evidence sessions",
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/ManoharEldhandi/model-worklog.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b --clean && tsc -b",
    "prepack": "npm run build",
    "test": "npm run build && tsc -p tsconfig.test.json && node --enable-source-maps --test \"test-dist/**/*.test.js\""
  },
  "dependencies": {
    "model-worklog-schema": "^0.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
