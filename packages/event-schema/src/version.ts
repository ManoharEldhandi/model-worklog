/** Wire-format versions shared by the SDK, supervisor, and clients. */
export const SCHEMA_VERSION = 1 as const;
export const API_VERSION = '1.0' as const;
export const SUPPORTED_API_MAJOR = 1 as const;
